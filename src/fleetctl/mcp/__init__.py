"""MCP binding over the agent toolkit.

Optional: `pip install fleetctl[mcp]`. Nothing in `core`, `packs` or `apps`
imports this, so a fleet that never talks to an agent never installs it.
"""

from __future__ import annotations
